#pragma once
#include <unordered_map>
#include <cstdint>
#include <Renderer.hpp>
#include <Model.hpp>

struct Model_h {
  uint16_t idx;
  uint16_t gen;
};

struct ManagedModel {
};

class ModelManager {
  
};
